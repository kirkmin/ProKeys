<h1>Sup this is customize</h1>
<ul id="keysets">
    <% keysets.each(function (keyset) { %>
        <li data-keyset-id="<%= keyset.escape('id') %>">
            <button>
                <%= keyset.escape('title') %>
            </button>
        </li>
    <% }); %>
</ul>

<div class="scroller" id="piano">
</div>
<div class="scroller" id="keyboard">
</div>